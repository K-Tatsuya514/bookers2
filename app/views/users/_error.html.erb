<% if @user.errors.any? %>
<div class="error-content">
	<div id="error_explanation" class="alert alert-danger">
		<h2 class="error-title"><%= pluralize(@user.errors.count, "error") %> prohibited this article from being seved:</h2>
		<ul class="error-text">
			<% @user.errors.full_messages.each do |message| %>
			<li><%= message %></li>
			<% end %>
		</ul>
	</div>
</div>
<% end %>